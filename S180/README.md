# API Migration Evaluation (Legacy v1 -> v2)

This repository contains two projects that simulate an API migration from /api/v1/checkStock to /api/v2/stock/availability.

Run `./run_all.sh` to execute pre-change and post-change tests, generate metrics, and create a compare report.

See individual project READMEs for per-project instructions.
