Pitfalls & mitigations:\n\n- Schema drift: enforce JSON schema validation and integration tests.\n- Missing params: add strict validation and feature flagging to enable fallback.\n- Async responses: implement polling with backoff or webhook subscription; ensure idempotent updates.\n- Timestamp formats: standardize on ISO 8601 and offset handling.\n- Observability: log incoming/outgoing payloads, track fallback counts via metrics.\n\nRollout plan: deploy v2 behind a feature flag, start with canary traffic, track fallback frequency and error rates, gradually increase traffic if metrics show stability.\n