Test cases (see top-level test_data.json)\n\n1. normal — verifies v2 with region/warehouse params returns confirmed availability. Maps params correctly.\n2. boundary_zero — verifies quantity 0 returns out of stock.\n3. async_pending — simulates v2 returning 'pending' then 'confirmed'; verifies polling handles async updates and the service eventually returns confirmed.\n4. invalid_input — checks missing regionId results in fallback to legacy API.\n5. v2_error — tests v2 500 error triggers fallback to v1 and service returns fallback decision.\n\nAcceptance criteria: Service uses v2 when USE_V2 set; fallback to v1 on errors or missing params; async handled by polling with limited retries; tests assert parity of user-facing availability decisions between pre and post-change.
